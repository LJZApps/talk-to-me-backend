{% extends 'base.html.twig' %}
{% set title = "Neuigkeiten" %}

{% block body %}
    <main class="container d-flex flex-row p-2">
        {% for article in articles %}
            <div class="card m-2" style="width: 20rem;">
                <div class="card-body">
                    <h5 class="card-title">{{ article.title }}</h5>
                    <p class="card-text">{{ article.text | slice(0, 100) }}</p>
                    <a href="#" class="btn btn-primary">Lesen</a>
                </div>
            </div>
            {#

            <!-- Card -->
            <div class="card smooth-shadow-md p-6">
                <!-- Card body -->
                <div class="card-body">
                    <h1 class="card-title text-black"><u>{{ article.title }}</u></h1>
                    {% apply markdown_to_html %}
                        {{ article.text | raw }}
                    {% endapply %}
                </div>
            </div>#}
        {% endfor %}
    </main>
{% endblock %}